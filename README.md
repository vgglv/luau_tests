# luau_tests

just learning how to embed luau, thats all

## key notes

* it seems that luau VM when doing `lua_ref` does not pop the stack
* there is no `luaL_ref`
* there is no `os` or `debug` or `io`
